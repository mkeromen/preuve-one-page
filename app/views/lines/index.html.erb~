<h1>Listing des lignes</h1>
  
<ul>
  <% @lines.each do |line| %>
    <li>
      <%= link_to line.title, :anchor => line.get_anchor %>
    </li>
  <% end%>
</ul>

  <% @lines.each do |line| %>
    <div id="<%= line.get_anchor%>">
      <h2><%= best_in_place line, :title %></h2>
      <p><%= best_in_place line, :text %></p>
    </div>
  <% end%>

<%= link_to "Ajouter une ligne", createplaceholder_path, id:"add-line", remote:true %>
